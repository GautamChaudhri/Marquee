// Registers `@testing-library/jest-dom` matchers (`toBeInTheDocument`,
// `toHaveTextContent`, ...) on Vitest's `expect` and installs Testing Library's
// automatic component cleanup between tests. Loaded by the `client` project.
import '@testing-library/jest-dom/vitest';
