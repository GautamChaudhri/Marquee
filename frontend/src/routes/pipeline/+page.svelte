<script lang="ts">
	import Stub from '$lib/components/Stub.svelte';
</script>

<Stub
	title="Review queue"
	subtitle="Poster selection pipeline"
	note="Run-based: trigger a run, watch per-run SSE, approve via /api/feedback (needs backend gap G4)."
/>
