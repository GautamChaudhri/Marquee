# 10 letterbox tv backend timeline

- completed: none
- in progress: phase 0 verification, legacy movie-query guard sweep, pre-schema snapshot coverage
- next steps: add `/api/letterbox/status` and `/api/letterbox/candidates` regression snapshots; add `media_type='movie'` guards to every legacy movie letterbox query/event path; implement schema migration and model updates for L1/L7/L8/L10
- deviations from the plan and why: full `pytest -q` baseline is currently blocked in this thread by workspace policy forbidding unsandboxed execution; sandboxed pytest cannot open the PostgreSQL socket required by `tests/conftest.py`
- pending operator actions: none yet

- 2026-07-04: verified prior checkpoint commit `6c769ec` only added movie snapshot coverage in `tests/test_letterbox.py`; no schema work started. Full baseline re-run succeeded unsandboxed on current `backend`: `740 passed, 32 failed` (baseline ceiling is now 32, not the older ~27 from Plan 08). `tests/test_letterbox.py::test_status_snapshot_preserves_movie_payload_shape` is already red before Plan 10 changes because the honored/not-honored client lists drifted from the frozen snapshot; treat that as pre-existing baseline. In progress: legacy `LetterboxState`/`LetterboxEvent` guard sweep and phase 0 schema/model patch. Next: add `media_type='movie'` guards everywhere legacy movie paths join/query letterbox tables, then implement the L1/L7/L8/L10 migration + ORM updates. Deviations: none from Plan 10 yet. Pending operator actions: none.
- 2026-07-04: completed phase 0 in commit `71a197b` (`add tv-ready letterbox schema`). Added `media_type`/`episode_id` subject generalization for `LetterboxState`, `LetterboxEvent`, and `LetterboxReencodeArtifact`; added provenance columns to `LetterboxState`; added Alembic migration `b1c2d3e4f5a6` with offline SQL verified via `alembic upgrade head --sql`; swept legacy movie queries/joins to require `media_type='movie'`; added a regression test proving `/api/letterbox/status` and `/api/letterbox/candidates` ignore episode-scoped rows. Focused verification: `ruff check marquee tests` clean; targeted letterbox test slice now `2 passed, 1 failed`, with the one failure still the pre-existing `/api/letterbox/status` snapshot drift on client support lists. In progress: phase 1 detection engine updates (proportional sampling, quick→thorough escalation, season triage, per-file fan-out). Next: read/update `letterbox_detect.py`, `letterbox_prefilter.py`, `letterbox_manager.py`, and `builtin_handlers.py` for TV batch orchestration. Deviations from the plan and why: provenance backfill derives `original_aspect_label` from the stored reencode plan JSON because existing artifact rows do not have a dedicated aspect-label column. Pending operator actions: none.
- 2026-07-04: completed phase 1 in progress-ready working tree (commit next): proportional TV sampling helpers in `letterbox_detect.py`, TV quick→thorough escalation in the detector, episode prefilter support in `letterbox_prefilter.py`, episode/media-file grouping + representative sampling helpers in `letterbox_manager.py`, and a new `letterbox_detect_episode` builtin handler for one-file-many-episodes fanout. Added focused tests for proportional offsets, TV escalation, representative episode selection, media-file grouping, and DB-backed multi-episode fanout. Focused verification: `ruff check marquee tests` clean; targeted phase-1 test slice `6 passed`. In progress: phase 2 service + sync changes (generalized subject handling, reset/provenance guard, heal extension) and the remaining season-triage orchestration needed before TV routes land. Next: extend `LetterboxService`, `sync_service.py`, and `letterbox_heal.py`; then build TV rollups/routes and route-order coverage. Deviations from the plan and why: no route/API surface yet in this phase; backend execution primitives were added first so later phases can reuse them instead of duplicating logic. Pending operator actions: none.
