# 12 letterbox tv refinements timeline

- completed: none
- in progress: not started
- next steps: chunk 1 (exhaustive semantics + library force pass-through)
- deviations from the plan and why: none
- pending operator actions: none

- 2026-07-09: completed chunk 1 (exhaustive semantics + library `force` pass-through). Exhaustive TV detect now rescans only `sampled_clear` episodes without forcing real verdict rows, `POST /api/letterbox/tv/detect` forwards `force` through parent and child payloads, the frontend TV library detect client accepts `force`, and focused regressions cover rerunning triaged seasons plus the library API payloads. Required backend gate is green: `ruff check marquee tests` clean and `pytest tests/test_letterbox.py tests/test_letterbox_tv_api.py` passed (`116 passed`). In progress: chunk 2 write-path/read-fallback aspect labels plus rollup guards. Next steps: implement the TV-only scanned-clear aspect-label write/read path, land the bar-bearing rollup guard in the same commit, and extend rollup/API coverage. Deviations from the plan and why: restored pre-existing backend gate drift before proceeding so the mandated slice could actually go green on `backend` — added movie-preview compatibility wrappers for renamed helpers, aligned the status snapshot expectation with the current route constants, and fixed episode-scoped TV apply to fan out across shared active media rows. Pending operator actions: none.
- 2026-07-09: completed chunk 2 (TV-only clear-row aspect labels + rollup guards in the same commit). `detect_episode_blocking` now stamps source-AR labels on `not_letterboxed` TV detections, TV read paths compute a fallback label for legacy clear rows with `last_detected_at`, `mark_tv_episode_not_letterboxed` resets the row to a source-AR clear invariant, and rollups/`/api/letterbox/summary` now count aspect labels only from bar-bearing buckets. Added unit, API, and rollup coverage for clear-label fallback behavior and the restricted dominant-label logic. Required backend gate is green: `ruff check marquee tests` clean and `pytest tests/test_letterbox.py tests/test_letterbox_tv_api.py tests/test_letterbox_rollups.py` passed (`126 passed`). In progress: chunk 3 `resolved_by` matrix payload fix. Next steps: emit `resolved_by` in `_episode_letterbox_row`, extend the payload-shape test, rerun the backend gate, and commit chunk 3. Deviations from the plan and why: none. Pending operator actions: none.
- 2026-07-09: completed chunk 3 (`resolved_by` in TV matrix rows). `_episode_letterbox_row` now emits `resolved_by` so the frontend Provenance column can render real values, and the TV detail payload-shape fixture now includes a `reencode`-resolved episode to lock that field in place. Required backend gate is green again: `ruff check marquee tests` clean and `pytest tests/test_letterbox.py tests/test_letterbox_tv_api.py tests/test_letterbox_rollups.py` passed (`126 passed`). In progress: chunk 4 frontend confidence-chip and episode-dropdown parity work. Next steps: port the confidence chip rendering, add the before/after dims + crop metadata + sample gallery to the TV episode dropdown, and run the frontend gate. Deviations from the plan and why: none. Pending operator actions: none.
- 2026-07-09: completed chunk 4 (TV confidence chip + dropdown parity). The TV detail page now renders categorical confidence chips instead of `NaN%`, shares `confidenceTone` through `$lib/display`, and the episode dropdown matches the movie detail parity surface with before/after dims + AR, crop top/bottom pixels, detect-method tags, variable-AR notes, and an exact-frame sample gallery that rebinds preview URLs by minute. Required frontend gate is green: `npm run check` passed with the repo's existing warnings only, `npm run lint` passed, and `npm run build` passed. In progress: chunk 5 per-season collapsible tables and multi-expand episode rows. Next steps: replace the single paginated episode table with season-scoped collapsible tables, move the season actions into each season header, keep heatmap navigation wired to the row IDs, and rerun the frontend gate. Deviations from the plan and why: included a formatting-only touch in `frontend/src/lib/api/letterbox.ts` so the repo-wide Prettier gate stayed green after splitting chunk 4 from chunk 5 in the same page file. Pending operator actions: none.
